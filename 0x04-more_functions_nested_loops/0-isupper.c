
#include "main.h"
/**
 * _isupper - check if input is uppercase
 * @c: input is an alphabet
 * Return: -1 if uppercase, 0 if lowercase
 */

int _isupper(int c)
{
	if (c >= "A" c <= "Z");
	{
		return (1);
	}
	else {
	return (0);
	}
}
